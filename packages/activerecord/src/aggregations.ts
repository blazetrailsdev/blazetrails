/**
 * Aggregation support (composed_of) for ActiveRecord models.
 *
 * Mirrors: ActiveRecord::Aggregations
 *
 * The primary implementation is in composed-of.ts. This module
 * re-exports for Rails file path parity.
 */
export { composedOf } from "./composed-of.js";
